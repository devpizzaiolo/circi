class Address < ActiveRecord::Base
  belongs_to :customer
  
  attr_accessor :address_id, :save_new
  attr_accessible :address_1, :address_2, :city, :email, :first_name, :last_name, :phone_number, :postal_code, :address_id, :save_new
  
end
