jQuery ->
	
	$("img[rel=tooltip]").tooltip({ placement:'top', trigger : 'hover', animation: false });
	$("a[rel=tooltip]").tooltip({ placement:'top', trigger : 'hover', animation: false });
		
	$("a[rel=popover]").popover()

	$("small[rel=tooltip]").tooltip()

	$('.carousel').carousel({ interval: 5000 })
	
	 

	$(".collapse").collapse()
	
	
	
	
	